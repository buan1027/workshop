# Workshop Server

Start repository for the upcoming server task.

## Status

The concrete requirements are still pending. This repository is intentionally minimal so the implementation can start cleanly once the task is announced.
